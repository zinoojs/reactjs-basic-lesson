import React from "react";

const HeroName = ({ hero }) => {
	// if (hero === "joker") {
	// 	throw new Error("Nor a Hero");
	// }
	return (
		<div>
			<h1>helo</h1>
			<p>{hero}</p>
		</div>
	);
};

export default HeroName;
